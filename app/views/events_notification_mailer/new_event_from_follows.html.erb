<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Nueva Notificación de Evento</title>
  </head>
  <body>
    <h1>Nuevo evento!</h1>
    <p>Hola <%= @user.username %>,</p>
    <p>Se ha creado un nuevo evento de un perfil que sigues</p>
    <h2><%= @event.name %></h2>
    <p><strong>Fecha:</strong> <%= @event.datetime %></p>
    <p><strong>Artista:</strong> <%= @event.artist.name %></p>
    <p><strong>Lugar:</strong> <%= @event.venue.name %></p>
    <p><strong>Productora:</strong> <%= @event.producer.name %></p>
    <% if @event.image.present? %>
      <%= image_tag @event.image.full_url %>
    <% end %>
    <p><%= link_to 'Ver evento', "#{REACT_HOST}/events/#{@event.id}" %></p>
  </body>
</html>
