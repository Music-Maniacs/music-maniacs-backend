<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Se realizaron cambios en un evento que sigues</title>
  </head>
  <body>
    <% @version.object_changes.each do |attr, values| %>
        <p><%= attr %>: <%= values[0] %> -> <%= values[1] %> </p>
    <% end %>
    <p><%= link_to 'Ver evento', "#{REACT_HOST}/events/#{@event.id}" %></p>
  </body>
</html>
